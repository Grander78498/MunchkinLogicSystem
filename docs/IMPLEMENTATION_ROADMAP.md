# Дорожная карта реализации логического языка SHLDN

> Полный план разработки лексера, парсера и функциональности для поддержки исчисления высказываний и исчисления предикатов

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [Текущее состояние](#текущее-состояние)
3. [Архитектура системы](#архитектура-системы)
4. [Фазы разработки](#фазы-разработки)
5. [Детальный план](#детальный-план)
6. [Критерии успеха](#критерии-успеха)
7. [Временные оценки](#временные-оценки)

---

## Обзор проекта

### Цель

Разработка полнофункционального логического языка программирования **SHLDN** для выполнения практических работ по дисциплине "Проектирование систем поддержки принятия решений".

### Требования

**Практическая работа №1 (Исчисление высказываний):**
- Лексер и парсер для логических выражений
- Операции алгебры логики (∧, ∨, ¬, →, ↔)
- База знаний (высказывания и аксиомы)
- Метод резолюций
- Преобразование в КНФ
- Функция help

**Практическая работа №2 (Исчисление предикатов):**
- Поддержка предикатов с аргументами
- Унификация переменных
- Термы (переменные для унификации)
- Правила вывода с кванторами
- Арифметические операции
- Операторы сравнения

### Ключевые возможности

✅ Высказывания и предикаты  
✅ Логические операторы (∧, ∨, ¬, →, ↔)  
✅ Унификация и подстановка переменных  
✅ Арифметические операции (+, -, *, /)  
✅ Операторы сравнения (>, <, >=, <=, ==, !=)  
✅ Метод резолюций  
✅ Обратный вывод (backward chaining)  
✅ Система команд (load, get, remove, help)  
✅ REPL-интерфейс  
✅ Обработка ошибок  

---

## Текущее состояние

### ✅ Реализовано

**Модели данных ([`models.py`](../models.py:1)):**
- Базовые классы: `Predicate`, `Variable`, `Term`
- Логические операции: `Conjunction`, `Disjunction`, `Negation`, `Implication`, `Equivalence`
- КНФ: `CNF`, `Disjunct`

**Логический движок ([`engine.py`](../engine.py:1)):**
- Преобразование в КНФ
- Метод резолюций для ИВ
- Законы де Моргана
- Удаление импликаций и эквиваленций

**Базовый лексер ([`lexer.py`](../lexer.py:1)):**
- Токенизация идентификаторов
- Поддержка строк в кавычках
- Базовая обработка ошибок

### ❌ Требуется реализация

1. **Расширение лексера** - поддержка всех конструкций языка
2. **Парсер** - построение AST из токенов
3. **Предикаты** - полная поддержка предикатов с аргументами
4. **Унификация** - алгоритм Робинсона
5. **Арифметика** - вычисление выражений
6. **Команды** - система управления БЗ
7. **REPL** - интерактивный интерфейс
8. **Тесты** - покрытие всех компонентов
9. **Документация** - справочник по языку

---

## Архитектура системы

### Компоненты

```
┌─────────────┐
│ Пользователь│
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────┐
│         REPL Interface              │
│  - Чтение ввода                     │
│  - Вывод результатов                │
│  - Обработка команд                 │
└──────┬──────────────────────────────┘
       │
       ├──────────────┬─────────────────┐
       ▼              ▼                 ▼
┌──────────┐   ┌──────────┐    ┌──────────────┐
│  Lexer   │   │  Parser  │    │   Commands   │
│          │   │          │    │              │
│ Токены   │──▶│   AST    │    │ load, get,   │
│          │   │          │    │ remove, help │
└──────────┘   └────┬─────┘    └──────┬───────┘
                    │                  │
                    ▼                  │
            ┌───────────────┐          │
            │ AST Transform │          │
            │               │          │
            │ Models        │          │
            └───────┬───────┘          │
                    │                  │
                    ▼                  ▼
            ┌─────────────────────────────┐
            │    Logical Engine           │
            │  - Унификация               │
            │  - Метод резолюций          │
            │  - Обратный вывод           │
            │  - Арифметика               │
            └─────────────────────────────┘
```

### Поток данных

```
Ввод → Лексер → Токены → Парсер → AST → Трансформер → Модели → Движок → Результат
```

---

## Фазы разработки

### Фаза 1: Расширение лексера (1-2 недели)

**Задачи:**
- Добавить все типы токенов (операторы, скобки, числа, переменные)
- Реализовать обработку комментариев (`//`, `/* */`)
- Добавить парсинг чисел и строк
- Различать идентификаторы и переменные (по первой букве)
- Обработка многосимвольных операторов (`->`, `==`, `>=`)
- Информативные сообщения об ошибках с позицией

**Результат:** Полнофункциональный лексер, покрытый тестами

### Фаза 2: Разработка парсера (1-2 недели)

**Задачи:**
- Определить грамматику языка в EBNF
- Создать классы AST узлов
- Реализовать рекурсивный спуск для каждого уровня грамматики
- Обработка приоритетов операторов
- Парсинг предикатов с аргументами
- Парсинг арифметических выражений
- Обработка ошибок парсера

**Результат:** Парсер, строящий корректное AST

### Фаза 3: Расширение моделей (1 неделя)

**Задачи:**
- Расширить `Predicate` для поддержки аргументов
- Улучшить `Variable` и `Term`
- Добавить классы для арифметики
- Добавить классы для сравнений
- Реализовать методы `is_ground()`, `get_variables()`
- Корректное сравнение и хеширование

**Результат:** Полная модель данных для ИП

### Фаза 4: Унификация (1-2 недели)

**Задачи:**
- Реализовать класс `Substitution`
- Реализовать `occurs_check()`
- Реализовать алгоритм унификации Робинсона
- Обработка вложенных предикатов
- Унификация списков предикатов
- Тесты для всех случаев унификации

**Результат:** Работающая унификация

### Фаза 5: Арифметика (1 неделя)

**Задачи:**
- Вычисление арифметических выражений
- Операторы сравнения
- Интеграция в правила
- Обработка ошибок (деление на ноль)

**Результат:** Поддержка арифметики в правилах

### Фаза 6: Система команд (1 неделя)

**Задачи:**
- Реализовать команды: load, get axioms, get facts, remove, help
- Создать `CommandRegistry`
- Интеграция с движком
- Тесты для команд

**Результат:** Работающая система команд

### Фаза 7: REPL-интерфейс (1 неделя)

**Задачи:**
- Создать класс `REPL`
- Интерактивный режим
- Режим выполнения файлов
- Обработка ошибок
- Красивый вывод результатов

**Результат:** Полнофункциональный REPL

### Фаза 8: Расширение движка (1-2 недели)

**Задачи:**
- Добавить поддержку фактов
- Реализовать `query()` с унификацией
- Реализовать backward chaining
- Доказательство конъюнкций
- Интеграция арифметики

**Результат:** Движок для ИП

### Фаза 9: Обработка ошибок (1 неделя)

**Задачи:**
- Иерархия исключений
- Валидация входных данных
- Информативные сообщения
- Отладочный режим

**Результат:** Надёжная обработка ошибок

### Фаза 10: Тестирование (1-2 недели)

**Задачи:**
- Модульные тесты для всех компонентов
- Интеграционные тесты
- Покрытие кода >80%
- Исправление багов

**Результат:** Стабильная система

### Фаза 11: Документация (1 неделя)

**Задачи:**
- README с примерами
- Справочник по языку
- API документация
- Примеры использования
- Комментарии в коде

**Результат:** Полная документация

---

## Детальный план

### Неделя 1-2: Лексер

**День 1-3: Расширение TokenType**
```python
# Добавить в lexer.py
class TokenType(str, Enum):
    # Идентификаторы
    IDENTIFIER = "identifier"
    VARIABLE = "variable"
    STRING = "string"
    NUMBER = "number"
    
    # Логические операторы
    AND = "AND"          # & или *
    OR = "OR"            # | или +
    NOT = "NOT"          # !
    IMPLIES = "IMPLIES"  # ->
    EQUIVALENCE = "EQUIVALENCE"  # == или <->
    
    # Арифметические операторы
    PLUS = "PLUS"
    MINUS = "MINUS"
    MULTIPLY = "MULTIPLY"
    DIVIDE = "DIVIDE"
    
    # Операторы сравнения
    GT = "GT"
    LT = "LT"
    GTE = "GTE"
    LTE = "LTE"
    EQ = "EQ"
    NEQ = "NEQ"
    
    # Разделители
    LPAREN = "LPAREN"
    RPAREN = "RPAREN"
    COMMA = "COMMA"
    QUESTION = "QUESTION"
    DOT = "DOT"
    
    # Служебные
    COMMENT = "COMMENT"
    EOL = "EOL"
    EOF = "EOF"
```

**День 4-7: Методы лексера**
- `skip_whitespace()` - пропуск пробелов
- `skip_comment()` - обработка комментариев
- `parse_number()` - парсинг чисел
- `parse_string()` - парсинг строк
- `parse_identifier_or_variable()` - различение идентификаторов и переменных
- `parse_operator()` - парсинг операторов

**День 8-10: Тесты**
- Тесты для каждого типа токена
- Тесты для комментариев
- Тесты для ошибок

### Неделя 3-4: Парсер

**День 11-14: AST узлы**
```python
# Создать parser.py
@dataclass
class PropositionNode(ASTNode):
    name: str

@dataclass
class PredicateNode(ASTNode):
    name: str
    args: list[ASTNode]

@dataclass
class VariableNode(ASTNode):
    name: str

@dataclass
class BinaryOpNode(ASTNode):
    op: str
    left: ASTNode
    right: ASTNode

@dataclass
class AxiomNode(ASTNode):
    condition: ASTNode
    consequence: ASTNode

@dataclass
class TheoremNode(ASTNode):
    expression: ASTNode
```

**День 15-20: Методы парсинга**
- `parse_expression()` - точка входа
- `parse_equivalence()` - эквиваленция
- `parse_implication()` - импликация
- `parse_disjunction()` - дизъюнкция
- `parse_conjunction()` - конъюнкция
- `parse_negation()` - отрицание
- `parse_primary()` - первичные выражения
- `parse_predicate()` - предикаты
- `parse_arithmetic()` - арифметика

**День 21-24: Тесты**
- Тесты для каждого уровня грамматики
- Тесты для предикатов
- Тесты для ошибок

### Неделя 5-6: Унификация и модели

**День 25-28: Расширение моделей**
```python
# Расширить models.py
class Predicate:
    def __init__(self, name: str, args: list | None = None):
        self.name = name
        self.args = args if args else []
    
    def is_ground(self) -> bool:
        """Проверка, является ли предикат основным"""
        ...
    
    def get_variables(self) -> set[str]:
        """Получение всех переменных"""
        ...
```

**День 29-32: Унификация**
```python
# Создать unification.py
class Substitution:
    def __init__(self, bindings: dict | None = None):
        self.bindings = bindings if bindings else {}
    
    def bind(self, var: str, value) -> "Substitution":
        ...
    
    def apply(self, pred: Predicate) -> Predicate:
        ...

def unify(x: Predicate, y: Predicate, subst: Substitution | None = None) -> Substitution | None:
    """Алгоритм унификации Робинсона"""
    ...
```

**День 33-36: Тесты**
- Тесты для моделей
- Тесты для унификации
- Тесты для подстановок

### Неделя 7-8: Интеграция

**День 37-40: Расширение движка**
```python
# Расширить engine.py
class LogicalEngine:
    def __init__(self):
        self.axioms: list[Disjunct] = []
        self.facts: list[Predicate] = []
        self.rules: list[tuple[Predicate, list[Predicate]]] = []
    
    def add_fact(self, predicate: Predicate):
        ...
    
    def query(self, goal: Predicate) -> list[Substitution]:
        ...
    
    def backward_chaining(self, goal: Predicate) -> list[Substitution]:
        ...
```

**День 41-44: Команды и REPL**
```python
# Создать commands.py
class LoadCommand:
    def execute(self, engine, args):
        ...

class GetAxiomsCommand:
    def execute(self, engine, args):
        ...

# Создать repl.py
class REPL:
    def run(self):
        while self.running:
            line = input("shldn> ")
            self.process_line(line)
```

**День 45-50: Финализация**
- Интеграционные тесты
- Исправление багов
- Документация
- Примеры

---

## Критерии успеха

### Функциональные требования

✅ **Исчисление высказываний:**
- Высказывания
- Логические операторы (∧, ∨, ¬, →, ↔)
- Аксиомы
- Теоремы
- Метод резолюций
- КНФ

✅ **Исчисление предикатов:**
- Предикаты с аргументами
- Термы (переменные)
- Унификация
- Факты и правила
- Обратный вывод

✅ **Арифметика:**
- Операции (+, -, *, /)
- Сравнения (>, <, >=, <=, ==, !=)
- Вычисление в правилах

✅ **Команды:**
- load - загрузка файла
- get axioms - вывод аксиом
- get facts - вывод фактов
- remove - удаление аксиомы
- help - справка

### Качественные требования

- **Покрытие тестами**: >80%
- **Производительность**: обработка 1000 фактов <1 сек
- **Обработка ошибок**: информативные сообщения
- **Документация**: полное описание
- **Примеры**: минимум 10 рабочих примеров

---

## Временные оценки

| Фаза | Задачи | Время |
|------|--------|-------|
| 1 | Расширение лексера | 1-2 недели |
| 2 | Разработка парсера | 1-2 недели |
| 3 | Расширение моделей | 1 неделя |
| 4 | Унификация | 1-2 недели |
| 5 | Арифметика | 1 неделя |
| 6 | Система команд | 1 неделя |
| 7 | REPL-интерфейс | 1 неделя |
| 8 | Расширение движка | 1-2 недели |
| 9 | Обработка ошибок | 1 неделя |
| 10 | Тестирование | 1-2 недели |
| 11 | Документация | 1 неделя |
| **Итого** | | **8-10 недель** |

### Критический путь (MVP)

**Минимальная функциональность (4-5 недель):**
1. Лексер для базовых конструкций (1 неделя)
2. Парсер для высказываний и аксиом (1 неделя)
3. Интеграция с существующим движком (1 неделя)
4. Простой REPL (1 неделя)
5. Базовые тесты (1 неделя)

---

## Следующие шаги

### Немедленные действия

1. ✅ Создать подробный план разработки
2. ⏭️ Начать расширение лексера
3. ⏭️ Написать тесты для лексера
4. ⏭️ Спроектировать структуру парсера
5. ⏭️ Определить грамматику языка

### Рекомендации

- **Итеративная разработка**: реализовывать по одной фазе
- **TDD подход**: писать тесты перед кодом
- **Частые коммиты**: сохранять прогресс
- **Документирование**: писать комментарии сразу
- **Рефакторинг**: улучшать код по мере роста

---

## Дополнительные материалы

### Полная документация

- [`development_plan.md`](development_plan.md) - Фазы 1-4 (Лексер, Парсер, Модели, Унификация)
- [`development_plan_part2.md`](development_plan_part2.md) - Фазы 5-8 (Арифметика, Команды, REPL, Движок)
- [`development_plan_part3.md`](development_plan_part3.md) - Фазы 9-17 (Ошибки, Тесты, Документация, Примеры)

### Примеры

См. [`example.shldn`](../example.shldn:1) для примеров синтаксиса языка

### Требования

- [`Практическая работа №1`](../Задания/Практическая работа №1 ПСППР.docx) - Исчисление высказываний
- [`Практическая работа №2`](../Задания/Практическая работа №2 ПСППР.docx) - Исчисление предикатов

---

**Версия**: 1.0  
**Дата**: 2025-12-04  
**Автор**: Yandex Code Assistant  
**Статус**: Готов к реализации