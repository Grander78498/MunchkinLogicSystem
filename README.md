# MunchkinLogic

Логический язык программирования на основе исчисления высказываний с полнофункциональным лексером и парсером.

## Описание

Реализация собственного логического языка программирования для практической работы №1 по дисциплине "Проектирование систем поддержки принятия решений".

### Возможности

- ✅ Полный набор логических операций: конъюнкция, дизъюнкция, импликация, эквиваленция, отрицание
- ✅ Преобразование выражений в конъюнктивную нормальную форму (КНФ)
- ✅ Метод резолюций для доказательства теорем
- ✅ Все законы Булевой алгебры
- ✅ Интерактивный консольный интерфейс (REPL)
- ✅ Поддержка файлов с высказываниями и аксиомами
- ✅ Обработка ошибок с понятными сообщениями

## Установка

Требуется Python 3.13 или выше.

```bash
git clone <repository-url>
cd MunchkinLogicSystem
```

## Использование

### Запуск интерактивного режима

```bash
python main.py
```

После запуска вы увидите приглашение `>>>` для ввода команд.

### Запуск демонстрации

```bash
python demo.py
```

### Синтаксис языка

#### Операторы
- `&` или `*` - конъюнкция (И)
- `|` или `+` - дизъюнкция (ИЛИ)
- `!` - отрицание (НЕ)
- `->` - импликация (ЕСЛИ...ТО)
- `<->` - эквиваленция (ТОГДА И ТОЛЬКО ТОГДА)
- `()` - скобки для группировки

#### Высказывания (элементы алфавита)
```
>>> игрок_1
>>> монстр_дракон
>>> "игрок имеет уровень 10"
```

#### Аксиомы (дизъюнкты Хорна)
```
>>> игрок_1_уровень_10 -> игрок_1_победитель
>>> a & b -> c
>>> !x | y -> z
```

#### Теоремы (проверка истинности)
```
>>> ? игрок_1_победитель
>>> ? a -> c
```

### Команды REPL

- `help` - показать справку по синтаксису и командам
- `get` - показать все высказывания и аксиомы
- `remove <id>` - удалить аксиому по ID
- `load <файл>` - загрузить файл с высказываниями и аксиомами
- `clear` - очистить базу знаний
- `exit` или `quit` - выйти из программы

### Комментарии

- `//` - однострочный комментарий
- `/* */` - многострочный комментарий

### Пример работы

```
>>> игрок_1
✓ Добавлено высказывание [1]: игрок_1

>>> игрок_1_уровень_10 -> игрок_1_победитель
✓ Добавлена аксиома (1): игрок_1_уровень_10 -> игрок_1_победитель

>>> ? игрок_1_победитель
======================================================================
ПРОВЕРКА ТЕОРЕМЫ: игрок_1_победитель
======================================================================

Аксиомы в базе знаний:
(1) игрок_1_уровень_10 -> игрок_1_победитель
...
```

## Структура проекта

```
MunchkinLogicSystem/
├── models.py              # Модели данных (Operation, Predicate, CNF, etc.)
├── engine.py              # Логический движок (преобразования, резолюции)
├── lexer.py               # Лексический анализатор
├── parser.py              # Синтаксический анализатор
├── knowledge_base.py      # База знаний
├── repl.py                # Консольный интерфейс
├── main.py                # Точка входа
├── demo.py                # Демонстрация работы
├── README.md              # Документация
├── tests/                 # Тесты
│   ├── test_lexer.py
│   ├── test_parser.py
│   ├── test_engine.py
│   └── test_kb.py
└── examples/              # Примеры использования
    ├── situation1.shldn
    ├── situation2.shldn
    └── situation3.shldn
```

## Тестирование

Для запуска тестов используйте pytest:

```bash
pytest
```

## Лицензия

MIT